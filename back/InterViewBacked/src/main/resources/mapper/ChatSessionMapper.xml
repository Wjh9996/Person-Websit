<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wjh.interviewbacked.mapper.ChatSessionMapper">

    <sql id="columns">
        id, user_id, title, deleted, created_at, updated_at
    </sql>

    <select id="selectByUser" resultType="ChatSession">
        SELECT <include refid="columns"/>
        FROM chat_session
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY updated_at DESC
    </select>

    <select id="selectById" resultType="ChatSession">
        SELECT <include refid="columns"/>
        FROM chat_session
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="ChatSession">
        INSERT INTO chat_session (id, user_id, title, deleted, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{title}, #{deleted}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="softDelete">
        UPDATE chat_session SET deleted = 1, updated_at = NOW() WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="updateTitle">
        UPDATE chat_session SET title = #{title}, updated_at = #{updatedAt} WHERE id = #{id}
    </update>
</mapper>
