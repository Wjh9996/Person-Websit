<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wjh.interviewbacked.mapper.UserMapper">

    <sql id="columns">
        id, username, password, nickname, email, avatar, bio, role, status, deleted, last_login_at, created_at, updated_at
    </sql>

    <select id="selectByUsername" resultType="User">
        SELECT <include refid="columns"/>
        FROM user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectByEmail" resultType="User">
        SELECT <include refid="columns"/>
        FROM user
        WHERE email = #{email} AND deleted = 0
    </select>

    <select id="selectById" resultType="User">
        SELECT <include refid="columns"/>
        FROM user
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" parameterType="User">
        INSERT INTO user (id, username, password, nickname, email, avatar, bio, role, status, deleted, last_login_at, created_at, updated_at)
        VALUES (#{id}, #{username}, #{password}, #{nickname}, #{email}, #{avatar}, #{bio}, #{role}, #{status}, #{deleted}, #{lastLoginAt}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="User">
        UPDATE user
        SET nickname   = #{nickname},
            email      = #{email},
            avatar     = #{avatar},
            bio        = #{bio},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="updateLastLogin">
        UPDATE user SET last_login_at = #{lastLoginAt}, updated_at = #{updatedAt} WHERE id = #{id}
    </update>
</mapper>
