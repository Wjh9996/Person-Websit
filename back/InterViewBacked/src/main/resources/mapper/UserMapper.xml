<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wjh.interviewbacked.mapper.UserMapper">

    <sql id="columns">
        id, username, password, nickname, email, avatar, bio, created_at
    </sql>

    <select id="selectByUsername" resultType="User">
        SELECT <include refid="columns"/>
        FROM user
        WHERE username = #{username}
    </select>

    <select id="selectById" resultType="User">
        SELECT <include refid="columns"/>
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="User">
        INSERT INTO user (id, username, password, nickname, email, avatar, bio, created_at)
        VALUES (#{id}, #{username}, #{password}, #{nickname}, #{email}, #{avatar}, #{bio}, #{createdAt})
    </insert>

    <update id="update" parameterType="User">
        UPDATE user
        SET nickname = #{nickname},
            email    = #{email},
            avatar   = #{avatar},
            bio      = #{bio}
        WHERE id = #{id}
    </update>
</mapper>
